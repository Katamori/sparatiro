let o = {
    "deps": {               // ----dependencies----
        "markdown": ""      // markdown parser
    },
    "paths": {              // ----file paths----
        "index": ""         // article index
    },
    "wikiName": "",         // ----global site used in header title----
    "html": {               // ----data inserted to HTML pages----
        "metadata": {       // metadata; obvious
            "name": {
                "author":  ""
            }
        }
    },
    "modulesRoot":  "",     // unused yet
    "modules": {

    }
}